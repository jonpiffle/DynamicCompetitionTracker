<%= form_for(@game_set) do |f| %>
  <% if @game_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_set.errors.count, "error") %> prohibited this game_set from being saved:</h2>

      <ul>
      <% @game_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<p>
<b>Games Played:</b>
<table>
  <thead>
    <tr>
      <th>Winner</th>
      <th>Loser</th>
    </tr>
  </thead>
  <tbody>
        <%@games.each do |game|%>
        <tr>
          <td><%=game.winner%></td>
          <td><%=game.loser%></td>
        </tr>
      <%end%>
  </tbody>
</table>
</p><br>

  <%= link_to "New Game", new_game_set_game_path(:game_set_id => @game_set.id) %>
<% end %>
